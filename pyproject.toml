[project]
name = "nvloader"
version = "0.1.0"
description = "video loading using nvdec"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "ipython>=8.39.0",
    "torch==2.7.1",
    "torchvision>=0.22.1",
]
